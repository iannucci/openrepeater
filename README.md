# OpenRepeater Webapp — W6EI fork (branch `2.1.3-bookworm`)

> **This is a fork.** Includes ~20 PHP 8.2 compatibility fixes,
> SVXLink 24.02 dBFS conversion for CW/CTCSS, deployed-commit SHA
> stamp in the footer, DTMF_CTRL_PTY emission for bench diagnostics,
> MODULE_PATH correction for arm64 Bookworm, and auto-save DB on
> "Rebuild & Restart". Used in the W6EI deployment (Palo Alto, CA).
>
> **Authoritative rebuild procedure:**
> [iannucci/openrepeater-config/BUILD.md](https://github.com/iannucci/openrepeater-config/blob/main/BUILD.md).
> Start there, not here. The `w6ei-YYYY-MM-DD` tags in all three
> related repos mark validated snapshots.

---

### Upstream original README follows

This is the GUI Application for the OpenRepeater project. By itself it does nothing. It requires a number of other dependencies and is built around a single board computer environment. This is more of a home for developers to update and share code. To get started with OpenRepeater, please visit [OpenRepeater.com](https://openrepeater.com) where you can download a precompiled image with everything you need to get started.
